# Finder A — general code review

You are reviewing `review-lab/roll_fleet.py`, a cron-driven automation that
starts production instance refreshes when a hardened AMI is approved.

Do a standard code review:

- Readability, naming, structure, and documentation
- Obvious logic errors or unhandled error paths
- Style and maintainability concerns

For each finding, give: a one-line summary, the function and line it lives
in, and a suggested fix. Rank findings by severity.

Do not look at `harness.py` scenarios, the answer key, or any other review's
output. Review the code on its own terms.
